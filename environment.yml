name: trident
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - ipywidgets
  - pytorch  # torch wird als pytorch in conda geführt
  - transformers
  - tqdm
  - h5py
  - matplotlib
  - segmentation-models-pytorch
  - opencv
  - openslide
  - openslide-python
  - pillow
  - timm=0.9.16
  - environs==11.0.0
  - geopandas
  - huggingface_hub
  - scipy
  - pip
  # ConceptCLIP (--patch_encoder conceptclip): weights from JerrryNie/ConceptCLIP (gated; HF token / local_ckpts.json)
  - pip:
      - einops_exts
      - pyisyntax
      - sacremoses==0.1.1
      - ftfy
      - regex
      - safetensors
